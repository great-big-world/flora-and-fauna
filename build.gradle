repositories {
    maven {
        url = uri "https://api.modrinth.com/maven"
        content {
            includeGroup "maven.modrinth"
        }
    }
}

loom {
    accessWidenerPath = file("src/main/resources/flora_and_fauna.accesswidener")
}

configurations {
    common {
        canBeResolved = true
        canBeConsumed = false
    }
    compileClasspath.extendsFrom common
    runtimeClasspath.extendsFrom common
}

dependencies {
    modCompileOnly "maven.modrinth:sodium:mc1.21.4-0.6.7-fabric"
    modCompileOnly "maven.modrinth:visuality:0.7.9+1.21.4"
    common(project(path: ':core', configuration: 'namedElements')) { transitive false }
}